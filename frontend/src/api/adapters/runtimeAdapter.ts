/**
 * TypeScript fallback. Vite aliases this module to the mode-specific runtime
 * adapter, so a production build only contains the selected implementation.
 */
export { createRuntimeAdapter } from '@/api/adapters/realRuntimeAdapter'
